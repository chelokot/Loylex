# The Terminal front page

This is a small React + Vite site that recreates the supplied mobile news-page reference.
The default route is intentionally static-looking, while the search, section menu, story
reader and My News controls make the page usable beyond the reference viewport.

Run it with bun install and bun run dev from this directory.

The GitHub Pages build uses /Loylex/ as its base path.
