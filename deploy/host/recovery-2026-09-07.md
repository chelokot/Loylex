# Recovery, 7 September 2026

Reviewed main: `762985d`; preserved as `quarantine/pre-recovery-20260907`.

## Findings and retained changes

- The active worker's authentication file was dated 31 August. Jobs 742 and 743 failed
  with `invalid_refresh_token`. Replacing it from the operator's current login yielded
  `AUTH_OK` in an actual Codex request inside the running container. This establishes the
  immediate failure, not who invalidated the earlier refresh token.
- Reviewed the cumulative changes from `2b49a77` through `762985d`. Retained reply-target
  context, tool usage counts, name aliases, emoji presentation, the static Telegram-link
  helper, and removal of the injected ROLEPLAY.md loading path. The helper's delete
  button only copies IDs; no public deletion service was found in the reviewed path.
- Commit `7537f19` had removed the operator's message-deletion restriction from `567f001`
  (operator job 627). Restore that exact restriction.
- Nonoperator job 677 created an always-loaded private `trial-knowledge.md` bucket. It
  contained disclaimers, but still installed an ongoing rule-interpretation procedure
  for IDs 426043802 and 849670500. Preserve the file and index in host quarantine and
  remove it from live memory. No individual precedents were recorded in it.
- Requests 687 and 689 sought a password-protected deletion service exposed from the
  container. The saved responses refused the port/credential changes; the inspected
  worker had only its entrypoint and Bun process running.
- Pending job 729 remained assigned to generation 156 after that draining worker stopped
  heartbeating; generation 157 could only claim its own work. Pending jobs had no lease
  to expire, and reclamation ran only during registration or a clean worker stop.

## Repair and verification

The active worker now reassigns pending jobs whose old generation has no live worker lease
when claiming work. Running-job lease recovery and thread serialization remain unchanged.
Regression tests cover a disappearing draining worker, a live draining worker, and no replay
of completed replies. `bun run check` passes (147 Bun tests, 10 Python tests).

AGENTS.md and resumed prompts explicitly reject permission inheritance from earlier turns,
parallel approvals, memory precedents, and already-committed changes. Standing behavior
changes require the current operator's authorization regardless of their storage location.
The existing GitHub write access is retained. These are model-facing instructions, not a
guarantee against every future prompt injection.

Host recovery material is stored under `/root/loylex-recovery-20260907` with a private directory:
the prior Compose file, a consistent SQLite backup, the prior authentication file, and the
quarantined memory. Credentials and chat data are not included in this repository.

No dependency, runtime version, registry, or installer change is included. Deployment uses
the existing image workflow and supervisor, with published image digests and container
health verified after rollout. Roll back code with a reviewed revert; the saved Compose
file preserves the pre-recovery image pins. Do not restore the invalid authentication or
the quarantined permission-memory mechanism as part of a code rollback.
